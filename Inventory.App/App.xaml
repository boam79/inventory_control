<Application x:Class="Inventory.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="ClinicHeaderBrush" Color="#2F3E4D"/>
        <SolidColorBrush x:Key="ClinicHeaderChipBrush" Color="#3D5164"/>
        <SolidColorBrush x:Key="ClinicCanvasBrush" Color="#EEF1F4"/>
        <SolidColorBrush x:Key="ClinicMenuBrush" Color="#F7F8FA"/>
        <SolidColorBrush x:Key="ClinicLineBrush" Color="#D8DEE4"/>
        <SolidColorBrush x:Key="ClinicSelectBrush" Color="#D9E4EE"/>
        <SolidColorBrush x:Key="ClinicAccentBrush" Color="#3D5A73"/>
        <SolidColorBrush x:Key="ClinicDangerBrush" Color="#B03A2E"/>
        <SolidColorBrush x:Key="ClinicWarnBrush" Color="#C47B16"/>
        <SolidColorBrush x:Key="ClinicWarnSoftBrush" Color="#FFF4E0"/>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C5CDD5"/>
        </Style>
        <Style x:Key="HeaderChip" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ClinicHeaderChipBrush}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="ShellMenuList" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Margin" Value="0,0,0,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#D9E4EE"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#3D5A73"/>
                                            <Setter TargetName="Bd" Property="BorderThickness" Value="3,0,0,0"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#E8EEF3"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
